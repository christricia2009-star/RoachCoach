fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
pydantic==2.9.2
python-dotenv==1.0.1
anthropic==0.34.2
openai==1.51.0
# openai==1.51.0's internal HTTP client construction passes a `proxies`
# kwarg that newer httpx releases (0.28+) removed, causing
# "Client.__init__() got an unexpected keyword argument 'proxies'" at
# runtime — pin httpx below that so pip doesn't silently grab a
# breaking version. If you bump openai to a newer release later, check
# whether this pin is still needed before removing it.
httpx<0.28
requests==2.32.3
cryptography==43.0.1
boto3==1.35.24
apscheduler==3.10.4

# Phase 5: payments. Stripe is live. Do not pin squareup here —
# 38.1.0.20241120 was never published and pip-fails the GitHub Action.
# Square stays optional in payments.py (ImportError -> PaymentError).
stripe==11.1.1
